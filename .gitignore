node_modules/
build/
dist/
coverage/
storybook-static/
.tools/
.mcp.json
.agents/

# PoC local de WhatsApp: não integra o app nem o deploy e pode conter
# credenciais de sessão e números reais usados em testes.
/whatsapp-test/

# Workflow de publicação npm criado fora da pasta de Actions. Este app é
# privado e é publicado pela Vercel, não pelo npm.
/src/deploy.yml

.vercel
.env*

# Estado local de ferramentas/agents e relatórios reproduzíveis de execução.
# Não pertencem ao código ou à configuração de produção. Exceção: subagentes
# do Claude Code em .claude/agents/ são configuração de time, versionada de
# propósito, para que qualquer pessoa usando Claude Code neste repositório
# tenha os mesmos agentes disponíveis.
.claude/*
!.claude/agents/
.claude-flow/
.swarm/
playwright-report/
test-results/
ruvector.db
bundle-metrics.json
skills-lock.json
CLAUDE.md
PRODUCT.md
