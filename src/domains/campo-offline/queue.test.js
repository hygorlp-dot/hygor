import { expect,it } from "vitest";import { enqueueOfflineOperation,resolveOfflineSync } from "./queue.js";it("não coloca financeiro na fila offline",()=>expect(enqueueOfflineOperation([],{type:"FINANCIAL_COMMAND",entityId:"x",idempotencyKey:"k"}).ok).toBe(false));it("mantém idempotência e conflito",()=>{const first=enqueueOfflineOperation([],{type:"DAILY_LOG_SAVE",entityId:"d",idempotencyKey:"k"});expect(enqueueOfflineOperation(first.queue,{type:"DAILY_LOG_SAVE",entityId:"d",idempotencyKey:"k"}).idempotent).toBe(true);expect(resolveOfflineSync(first.queue,{idempotencyKey:"k",ok:false})[0].status).toBe("conflito");});
