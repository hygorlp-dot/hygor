import { describe,expect,it } from "vitest";import { projectCashflow,contractBalances } from "./calculations.js";
describe("controladoria",()=>{it("projeta caixa sem transformar previsão em realizado",()=>{const result=projectCashflow([{date:"2026-07-01",direction:"saida",amountCents:150}],{openingBalanceCents:100});expect(result.capitalNeedCents).toBe(50);});it("separa saldo não faturado, a receber e recebido",()=>{const result=contractBalances({id:"c",valueCents:1000},[{contractId:"c",valueCents:600}],[{contractId:"c",amountCents:200}]);expect(result.unbilledCents).toBe(400);expect(result.openReceivableCents).toBe(400);});});
