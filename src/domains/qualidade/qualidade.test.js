import { describe,expect,it } from "vitest";import { canReleaseForMeasurement } from "./calculations.js";import { transitionNonconformity } from "./mutations.js";
describe("qualidade",()=>{it("impede medição com não conformidade impeditiva",()=>expect(canReleaseForMeasurement({obraId:"o",serviceId:"s",nonconformities:[{obraId:"o",serviceId:"s",impeditiva:true,status:"aberta"}]}).ok).toBe(false));it("exige justificativa e sequência na não conformidade",()=>{expect(transitionNonconformity({status:"aberta"},"contencao").ok).toBe(true);expect(transitionNonconformity({status:"verificacao"},"encerrada").ok).toBe(false);});});
